# 登录体验修复计划

## 范围与决定

- 关闭 iOS 注册入口，服务端默认禁止邮箱注册及 Apple 首次登录自动开户；已有账号登录、绑定及找回密码保持可用。
- 服务地址从构建配置读取，Debug 模拟器默认本机 21000，真机及 Release 默认 `https://measure-api.ydfk.site`。Debug 支持开发环境变量覆盖，Release 只接受 HTTPS，不再读取旧的手工地址。
- 延续 `docs/brand.md` 的测量弧线、蓝色系和系统字体；登录页使用正式品牌图形、全屏背景、原生输入框和清晰的主按钮，支持键盘滚动与动态字体。
- 补齐启动屏配置，解决旧式兼容显示造成的上下黑边；保留工作区现有 Xcode 项目和 Scheme 修改。

## 执行步骤

1. 已拉取远程并检查登录、品牌、认证服务和现有测试。
2. 修改认证开关、服务端拒绝响应与回归测试。
3. 修改 iOS 环境配置、启动屏、登录布局和品牌资源。
4. 更新配置及接口文档，执行 Go 测试、iOS 构建及登录 UI 测试，检查模拟器截图。

## 验收

- 新安装无需手填服务地址；登录页无注册或服务器设置入口。
- 禁止注册时接口返回 403，数据库不产生新账号；已有账号仍能登录。
- 登录页铺满屏幕、Logo 可见，键盘和大字体下仍可访问登录与密码恢复。
- 分别报告本地验证和真实服务器、Apple、真机的验证边界。

## 验证结果（2026-09-05）

- 已完成：Go 全量测试、`go vet ./...`、OpenAPI 3.0/3.1 生成、`git diff --check`。
- 已完成：iPhone 17 Pro Max / iOS 26.5 模拟器下 27 个单元测试、2 个 UI 测试；最后一次外观修正后再次通过 2 个 UI 测试。
- 已完成：Debug 与 Release 构建；核对包内 `UILaunchScreen` 及服务地址，分别为本机 21000 和正式 HTTPS 服务。
- 已检查：模拟器浅色、深色、最大辅助功能字号的换行；普通字号的键盘输入与密码恢复交互。
- 线上只读检查：正式地址 `/api/health` 返回 HTTP 404，未确认真实登录、Apple 或邮件能力；需要核对部署及反向代理路由。
- 未部署后端、未提交或推送；服务端注册开关需随本次后端更新部署后生效，真机与横屏体验仍需验证。
